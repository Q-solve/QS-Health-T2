# Used by Vite at build time (Vercel → Project Settings → Environment Variables).
# Point this at your Render web service URL (no trailing slash).
# Example: https://afyadeploy-api.onrender.com
VITE_API_BASE_URL=
